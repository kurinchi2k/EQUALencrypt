# Build app
docker compose up --build
# Check whether the app works in port 3838 by typing http://localhost:3838 in a browser
# Press Ctrl c after confirming that the previous step is working
docker build -t equalencrypt_columns:latest .
docker tag equalencrypt_columns kurinchi2k/equalencrypt_columns
# After this push this image to repository from Docker Desktop app